import 'package:flutter/material.dart';

class AppColors {

  static const hLColor = Colors.orange;
  static const supplementColor = Colors.red;

  static const defaultAppColor = Color(0xff008080);
  static const defaultAppColorDarker = Color(0xff003931);
  static const defaultAppColorDarkest = Color(0xff00322B);

  static const surfaceLightTheme = Color(
    0xFFEDEDED,
  ); // Slightly darker than 0xFFF5F5F5
  static const surfaceDarkTheme = Color(
    0xFF0E0E0E,
  ); // Slightly darker than 0xFF121212

  static const surfaceLightThemeStronger = Color(
    0xFFE2E2E2,
  ); // Even closer to white
  static const surfaceDarkThemeStronger = Color(
    0xFF181818,
  ); // Slightly lighter but still dark

  static const surfaceLightThemeGrey = Color(
    0xFFEEEEEE,
  ); // Soft grey, good for light theme
  static const surfaceDarkThemeGrey = Color(
    0xFF2A2A2A,
  ); // Muted dark grey, good for dark theme

  static const dividerLightTheme = Color(
    0xFFBDBDBD,
  ); // Much darker than the light theme background
  static const dividerDarkTheme = Color(0xFF050505);

  static const onSurfaceLightTheme = Color(0xff5c5e60);
  static const onSurfaceDarkTheme = Color(0xFFBDBDBD);

  static const shadowLightTheme = Color(0x1A000000);
  static const shadowDarkTheme = Color(0x1AFFFFFF);
}
